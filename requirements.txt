Flask==2.3.2
Flask-Cors==3.0.10
googletrans==4.0.0-rc1  # For Google Translate API (replace with your translation library if needed)
gunicorn==21.2.0         # For running the app in production environments
Jinja2==3.1.2            # For template rendering
Werkzeug==2.3.7          # For WSGI server support
itsdangerous==2.1.2      # Dependency for Flask
MarkupSafe==2.1.3        # Dependency for Jinja2
